name: trigger scheduled workflow
on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * * '
jobs:
  schedule_workflow:
    runs-on: 'ubuntu-latest'
    steps:
      - name: print-log
        run: |
          current_date_time=$(date)
          echo "Current Date and Time: $current_date_time"
