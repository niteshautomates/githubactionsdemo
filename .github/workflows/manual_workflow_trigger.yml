name: Manual workflow
on:
  workflow_dispatch:
    inputs:
      name: 
        description: 'this is test variable'
        type: string
        default: 'test'
    
jobs:
  demo: 
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello World"
